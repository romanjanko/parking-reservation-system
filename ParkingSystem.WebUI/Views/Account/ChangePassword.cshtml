@model ParkingSystem.WebUI.Models.ChangePasswordViewModel

@{
    ViewBag.Title = "Change Password";
}

<h4>Change password form.</h4>
<p>
    To change your password, enter old one and type in new password which needs to be retyped in the last field.
</p>
<br />

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        @if (ViewData.ModelState.Any(x => x.Value.Errors.Any()))
        {
            <div class="row">
                <div class="col-sm-offset-4 col-sm-4">
                    @Html.ValidationSummary(false, "", new { @class = "alert alert-danger" })
                </div>
            </div>
        }
        <div class="form-group">
            <div class="col-sm-offset-4 col-sm-4">
                <div class="input-group">
                    <span class="input-group-addon"><span class="fa fa-key fa-fw"></span></span>
                    @Html.PasswordFor(m => m.OldPassword,
                                      htmlAttributes: new
                                      {
                                          @class = "form-control",
                                          placeholder = "Current password"
                                      })
                </div>
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-offset-4 col-sm-4">
                <div class="input-group">
                    <span class="input-group-addon"><span class="fa fa-key fa-fw"></span></span>
                    @Html.PasswordFor(m => m.NewPassword,
                                      htmlAttributes: new
                                      {
                                          @class = "form-control",
                                          placeholder = "New password"
                                      })
                </div>
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-offset-4 col-sm-4">
                <div class="input-group">
                    <span class="input-group-addon"><span class="fa fa-key fa-fw"></span></span>
                    @Html.PasswordFor(m => m.ConfirmPassword,
                                      htmlAttributes: new
                                      {
                                          @class = "form-control",
                                          placeholder = "Confirm new password"
                                      })
                </div>
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-offset-4 col-sm-4">
                <p class="text-center">
                    <input type="submit" value="Change Password" class="btn btn-primary btn-lg" />
                </p>
            </div>
        </div>
    </div>
}